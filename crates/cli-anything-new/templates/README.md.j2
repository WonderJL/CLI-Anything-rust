# {{ crate_name }}

{{ description }}

> Generated by `cli-anything-new`. The harness boilerplate (clap surface,
> reedline REPL, `--json` envelope, auto-save, SKILL.md emitter) is in place;
> fill in the domain logic in `src/domain/` and the real backend calls in
> `src/backend.rs`.

## Prerequisites

- The real **{{ software_display }}** backend on `PATH` (see `src/backend.rs`).

## Build & install

```bash
cargo build -p {{ crate_name }}
cargo install --path crates/{{ crate_name }}   # puts `{{ crate_name }}` on PATH
```

## Usage

```bash
{{ crate_name }} project new --title "demo"      # one-shot
{{ crate_name }} --json session status           # machine-readable
{{ crate_name }}                                  # no subcommand → REPL
```

- Every command supports `--json`. Output is a uniform envelope:
  `{ok, action, data, error, warnings}` (`ok=false` + `error.kind/message/hint`
  on failure).
- Global flags: `--project <path>`, `--dry-run`.
- One-shot mutations auto-save to the open project unless `--dry-run`.

## Regenerate SKILL.md

```bash
{{ crate_name }} emit-skill -o crates/{{ crate_name }}/SKILL.md
```

The SKILL is derived from the live clap command tree, so it never drifts.

## Tests

```bash
cargo test -p {{ crate_name }}                 # unit tests
cargo test -p {{ crate_name }} -- --ignored    # e2e (needs the real backend)
```
